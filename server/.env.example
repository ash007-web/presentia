PORT=5000
NODE_ENV=development
# Firebase Admin SDK — place serviceAccountKey.json in server/ directory
# OR set FIREBASE_PROJECT_ID to use Application Default Credentials
FIREBASE_PROJECT_ID=presentia-643fc
